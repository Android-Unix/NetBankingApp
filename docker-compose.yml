version: '3'

volumes:
  django-logs:
    driver: local

services:

  netbankingapp:
    build: ~/Djangoprojects/NetBankingApp/.

    volumes:
      - ~/Djangoprojects/NetBankingApp:/home/NetBankingApp/
    ports:
      - "8000:8000"

    command:
      "python manage.py runserver 0.0.0.0:8000"
